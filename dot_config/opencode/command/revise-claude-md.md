---
description: Propose concise CLAUDE.md updates from the current session
---

Review this session for durable lessons that would help future coding-agent sessions work more effectively in this repository.

1. Identify missing or outdated context revealed by this session, including commands, code patterns, testing approaches, configuration quirks, and recurring gotchas.
2. Locate relevant `CLAUDE.md` and `.claude.local.md` files using Glob. Use `CLAUDE.md` for shared guidance and `.claude.local.md` for personal guidance.
3. Exclude generic advice, obvious facts, secrets, host-specific values, and one-off fixes unlikely to recur.
4. Draft concise additions, preferably one line per concept.
5. Show each proposed change as a diff with a one-line reason.
6. Ask for approval and edit only the files and changes the user approves.

Do not modify any instruction file before receiving approval.
